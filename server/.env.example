# Example .env - copy to server/.env and edit
PORT=80
# HLS output folder inside container (mounted to host ./server/hls)
HLS_DIR=/app/hls
# (Optional) max ffmpeg runtime in seconds (0 = unlimited)
FFMPEG_TIMEOUT=0
